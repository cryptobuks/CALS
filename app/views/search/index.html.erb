<div id="search_page">
  <%= react_component('search', {
  	user: @user,
  	landingUrl: @landingPage_url,
  	countyTypes: @dictionaries[:county_types],
  	facilityTypes: @dictionaries[:facility_types],
  	from: @from ? @from : 0,
  	size: @size ? @size : 5,
  	pageNumber: @page_number ? @page_number : 1,
  	inputData: @input_data ? @input_data : {},
  }) %>
  <%= javascript_pack_tag 'search' %>
  <%= stylesheet_pack_tag 'search' %>
</div>
